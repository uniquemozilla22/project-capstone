/**
 *
 *
 * This  folder is to define the context for the application that
 * you can use to manage your states and functionality
 * throughout the application or a single instance of the application
 *
 *
 */

import { createContext } from "react";
import { DataContext } from "./context";

export const DataContext = createContext({});

export default DataContext;
